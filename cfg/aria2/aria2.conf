# aria2 defaults. Keep secrets and download paths out of git.
# Optional local overrides: aria2.local.conf (gitignored).

continue=true
max-connection-per-server=5
min-split-size=10M
split=6
timeout=60

# RPC on loopback only. Set rpc-secret in a local config if you enable this.
enable-rpc=true
rpc-listen-all=false
rpc-allow-origin-all=false

# BitTorrent
listen-port=51413
enable-dht=true
enable-peer-exchange=true
seed-ratio=1.0
bt-save-metadata=true
